{
  "name": "model-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch services/dev-server/src/index.js",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "start": "node --env-file-if-exists=.env services/dev-server/src/index.js",
    "lint": "node scripts/validate-workspace.mjs"
  },
  "devDependencies": {},
  "dependencies": {}
}
